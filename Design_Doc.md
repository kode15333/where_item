\*\*React Native(Expo)\*\*와 \*\*AI Editor\*\*를 사용하여 개발하기 위한 \*\*Technical Design Document (TDD)\*\*입니다.


-----

# 📱 Project Design Doc: "거기 뒀다 (WhereIsIt)"

### 1\. 프로젝트 개요 (Overview)

  * **목적:** 노인층을 포함한 모든 연령대가 물건의 위치를 직관적(사진 중심)으로 기록하고 기억할 수 있는 앱.
  * **핵심 원칙:**
      * **No Login:** 로컬 스토리지 기반 (서버 없음).
      * **Visual First:** 텍스트 최소화, 사진과 아이콘 위주.
      * **Object Centric:** 물건을 먼저 선택하고 위치를 업데이트하는 '서랍장' 메타포.

### 2\. 기술 스택 (Tech Stack)

Cursor가 코드를 생성할 때 사용할 구체적인 라이브러리를 지정합니다.

  * **Framework:** React Native (Expo SDK 50+)
  * **Language:** TypeScript
  * **Styling:** NativeWind (Tailwind CSS for RN) - *빠른 UI 개발*
  * **State Management & Persistence:** Zustand + AsyncStorage (JSON 저장)
  * **Camera & Image:** `expo-camera`, `expo-image-manipulator`, `expo-file-system`
  * **Icons:** Lucide React Native (직관적인 아이콘)
  * **Navigation:** Expo Router (File-based navigation)

### 3\. 데이터 모델 (Data Schema)

서버가 없으므로 로컬 `AsyncStorage`에 JSON 형태로 저장될 데이터 구조입니다.

```typescript
// types.ts

interface Item {
  id: string;             // UUID
  name: string;           // 사용자 입력 이름 (예: "차키", "강아지목줄")
  type: 'preset' | 'custom';
  iconUri: string;        // 프리셋 아이콘 이름 또는 사용자가 찍은 크롭된 사진 경로
  locationImageUri: string | null; // 가장 최근 찍은 위치 사진 경로 (로컬 파일 시스템)
  updatedAt: number;      // 타임스탬프 (최근 갱신일 확인용)
}

interface AppState {
  items: Item[];
  addItem: (item: Item) => void;
  updateLocation: (id: string, newImageUri: string) => void;
  deleteItem: (id: string) => void;
}
```

### 4\. 핵심 기능 명세 (Core Features)

#### A. 메인 화면 (Grid View)

  * **UI:** 2열 또는 3열 그리드 레이아웃.
  * **아이템 카드:**
      * 큰 아이콘 (또는 물건 사진).
      * 하단에 작은 텍스트 (이름).
      * 배경색: 최근 24시간 내 업데이트됨(밝음) / 오래됨(약간 어두움)으로 시각적 구분.
  * **추가 버튼:** 스크롤 최하단 또는 우측 하단 Floating Button (+).

#### B. 물건 등록 프로세스 (Add Flow)

1.  **입력:** 물건 이름 입력 (필수 아님, 기본값 제공).
2.  **아이콘 선택:**
      * 옵션 1: 기본 아이콘 (열쇠, 지갑, 안경 등 프리셋).
      * 옵션 2: **"사진 찍어 아이콘 만들기"** (카메라 실행 -\> 사진 촬영 -\> 중앙 원형 크롭 -\> 아이콘으로 저장).

#### C. 위치 업데이트 프로세스 (Update Flow)

1.  **진입:** 메인에서 아이템 탭.
2.  **상세 모달:** 현재 저장된 위치 사진이 크게 보임.
3.  **액션:** 하단 대형 버튼 [여기로 옮겼어요 (카메라)] 클릭.
4.  **촬영:** 카메라 실행 -\> 촬영 -\> 즉시 저장 및 덮어쓰기 (히스토리 X, 현재 위치 O).

### 5\. 파일 디렉토리 구조 (Directory Structure)

```
/app
  ├── index.tsx          // 메인 그리드 화면
  ├── _layout.tsx        // 네비게이션 설정
  ├── add-item.tsx       // 물건 추가 화면
  └── item/[id].tsx      // 물건 상세 및 업데이트 화면
/components
  ├── ItemGrid.tsx
  ├── ItemCard.tsx
  └── CameraView.tsx
/store
  └── useStore.ts        // Zustand 스토어 (persist 포함)
/assets
  └── presets/           // 기본 아이콘 이미지들
```

-----